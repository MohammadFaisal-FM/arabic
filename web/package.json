{
  "name": "najdi-arabic-pwa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "dev": "npm run sync && vite",
    "build": "npm run sync && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vite-plugin-pwa": "^0.21.1"
  },
  "dependencies": {
    "marked": "^15.0.6"
  }
}
