{
  "name": "arabic-pwa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "roots": "node scripts/generate-roots.mjs",
    "vocab": "node scripts/generate-vocab.mjs",
    "content": "npm run roots && npm run vocab && npm run sync",
    "dev": "npm run content && vite",
    "build": "npm run content && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vite-plugin-pwa": "^0.21.1"
  },
  "dependencies": {
    "marked": "^15.0.6"
  }
}
